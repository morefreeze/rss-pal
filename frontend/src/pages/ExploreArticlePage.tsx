// Task 11 owns the candidate reader UI. This route placeholder deliberately
// has no fetch or subscription behavior so opening it cannot subscribe a feed.
export default function ExploreArticlePage() {
  return null
}
