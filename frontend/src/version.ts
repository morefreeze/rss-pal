// Frontend build version (semver-style: vMAJOR.MINOR.PATCH). At the start
// of a dev cycle, bump the patch and append .alpha (e.g. v0.0.1 ->
// v0.0.2.alpha). Right before merging to master, drop the .alpha suffix
// (e.g. v0.0.2.alpha -> v0.0.2).
export const VERSION = 'v0.0.1'
