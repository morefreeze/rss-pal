# 订阅默认私有与可见范围说明

用户授权：将管理员的共享订阅改为私有，只保留推荐订阅公开；指出后台共享设置不明确。

## 规则
- 管理员与普通用户通过添加订阅、OPML 导入、单次链接集合创建的订阅均归当前用户所有。
- 保留现有公共推荐机制与推荐目录，不新增自动公开或共享开关。
- 当前生产的 28 个共享订阅中，23 个不在页面热门推荐 URL 名单中，已备份并归属管理员 ID 1；5 个与热门推荐重合，暂保留公开。用户若确认目录公开但不自动订阅，再将剩余 5 个私有化。
- 订阅页明确标注“私有 · 仅自己可见”或“共享 · 所有用户可见”，添加区域解释所有账号默认私有。
- 不改变公开文章分享快照，不删除文章或阅读记录。

## 验证
- 普通测试账号 ID 5 已真实注册登录，修改前看到 28 个共享订阅。
- 真实数据库 HTTP 回归：管理员创建后的 owner_id 为本人，另一个用户无法读取；普通用户同样默认私有。
- 全部后端测试、前端检查与构建；部署准确提交并复核普通用户页面、推荐入口和服务健康。
