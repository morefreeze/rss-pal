# 订阅默认私有实施计划

**目标：** 避免管理员添加个人订阅时自动公开，明确页面可见范围。
**架构：** 沿用 owner_id + RLS，将 getOwnerID 统一返回当前用户；推荐目录保留；生产已有共享订阅按明确名单归属管理员。

- [x] 新增真实数据库 HTTP 测试，复现管理员创建订阅后被其他用户读取。
- [x] 修正默认 owner_id，确认新增订阅和单次链接集合均使用此规则。
- [x] 更新订阅管理的可见范围文案及项目约定。
- [x] 后端全量与前端检查构建，审阅差异。
- [ ] 合并推送部署腾讯，核对真实测试账号页面与健康。
